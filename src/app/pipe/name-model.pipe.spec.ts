import { NameModelPipe } from './name-model.pipe';

describe('NameModelPipe', () => {
  it('create an instance', () => {
    const pipe = new NameModelPipe();
    expect(pipe).toBeTruthy();
  });
});
